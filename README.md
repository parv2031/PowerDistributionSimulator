# PowerDistributionSimulator
A C++ &amp; raylib power grid simulation. This project procedurally generates a dynamic graph of power plants, substations, and consumers. It's an interactive sim where users can trigger overloads (red arrows) and watch the system automatically reroute power to fix them in real-time (green arrows).
